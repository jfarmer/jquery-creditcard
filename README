A helper class for credit card number fields.

Currently only detects a card type and adds the appropriate class (for styling).

TODO:
Luhn Check
Misc. validation (card length, etc.)
Custom callback on match
